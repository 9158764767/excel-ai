# Copy this file to `.env` and configure values for deployment. These variables
# can also be exported in your shell or configured via your deployment
# platform's environment settings.

# OpenAI API key used by the backend to call the Responses API with the
# Code Interpreter tool. Required.
OPENAI_API_KEY=sk-REPLACE_ME

# Model to use. Defaults to gpt-4o if not set.
OPENAI_MODEL=gpt-4o

# Require an app key header for authentication (`X-App-Key` header). Set to 1
# to enable. Leave empty or set to 0 to disable.
REQUIRE_APP_KEY=1

# Application key value expected in the `X-App-Key` header when
# `REQUIRE_APP_KEY=1`. Change this to a secret string.
APP_KEY=change-this-key
